# 開發工作流程 Development Workflow

## 🎯 目標
在本地 Browser Preview 中開發和測試新功能，確認無誤後再合併到主分支並發布到 Vercel。

---

## 📋 推薦工作流程

### 方法 1: Git 分支策略（推薦）

#### 步驟 1: 創建開發分支
```bash
# 從 main 分支創建新的功能分支
git checkout -b feature/新功能名稱

# 例如：
git checkout -b feature/notification-system
```

#### 步驟 2: 在本地開發和測試
```bash
# 啟動開發伺服器
npm run dev

# 在 Browser Preview 中測試
# 網址通常是：http://localhost:3000
```

#### 步驟 3: 提交更改
```bash
# 查看更改
git status

# 添加更改的檔案
git add .

# 提交更改（帶描述性訊息）
git commit -m "feat: 新增通知系統功能"

# 推送到遠端（第一次推送需要設定 upstream）
git push -u origin feature/notification-system
```

#### 步驟 4: 測試完成後合併到 main
```bash
# 切換回 main 分支
git checkout main

# 拉取最新的 main 分支（確保是最新的）
git pull origin main

# 合併功能分支
git merge feature/notification-system

# 推送到遠端（這會觸發 Vercel 自動部署）
git push origin main
```

#### 步驟 5: 清理分支（可選）
```bash
# 刪除本地分支
git branch -d feature/notification-system

# 刪除遠端分支
git push origin --delete feature/notification-system
```

---

### 方法 2: 直接在 main 分支開發（簡單功能）

#### 適用場景
- 小修改、bug 修復
- 文檔更新
- 樣式調整

#### 步驟
```bash
# 1. 確保在 main 分支
git checkout main
git pull origin main

# 2. 本地開發和測試
npm run dev

# 3. 測試完成後提交
git add .
git commit -m "fix: 修復卡片排序問題"
git push origin main
```

---

## 🔄 完整開發流程圖

```
開始新功能
    ↓
創建功能分支 (feature/xxx)
    ↓
本地開發 (npm run dev)
    ↓
Browser Preview 測試
    ↓
有問題？
    ├─ 是 → 繼續修改
    └─ 否 ↓
提交到功能分支
    ↓
測試完成？
    ├─ 否 → 繼續測試
    └─ 是 ↓
合併到 main 分支
    ↓
推送到 GitHub
    ↓
Vercel 自動部署
    ↓
完成！
```

---

## 🛠️ 實用 Git 指令

### 查看分支
```bash
# 查看所有本地分支
git branch

# 查看所有分支（包含遠端）
git branch -a
```

### 切換分支
```bash
# 切換到指定分支
git checkout 分支名稱

# 或使用新語法
git switch 分支名稱
```

### 查看更改
```bash
# 查看未提交的更改
git status

# 查看具體更改內容
git diff

# 查看提交歷史
git log --oneline
```

### 撤銷更改
```bash
# 撤銷未提交的更改（危險！）
git restore 檔案名稱

# 撤銷所有未提交的更改
git restore .

# 撤銷最後一次提交（保留更改）
git reset --soft HEAD~1
```

---

## 🧪 本地測試檢查清單

在合併到 main 之前，確認：

- [ ] 功能在 Browser Preview 中正常運作
- [ ] 沒有 TypeScript 錯誤（`npm run build` 成功）
- [ ] 沒有 console 錯誤
- [ ] 不影響現有功能
- [ ] 響應式設計正常（不同螢幕尺寸）
- [ ] 資料同步正常（Supabase、Google Sheets）

---

## 🚀 Vercel 部署流程

### 自動部署
當你推送到 `main` 分支時，Vercel 會自動：
1. 偵測到新的 commit
2. 開始建置
3. 部署到生產環境

### 預覽部署
當你推送到其他分支時，Vercel 會：
1. 創建預覽部署
2. 提供一個臨時的預覽 URL
3. 不會影響生產環境

### 手動部署（可選）
```bash
# 使用 Vercel CLI
vercel --prod
```

---

## 📝 提交訊息規範

### 格式
```
類型: 簡短描述

詳細說明（可選）
```

### 類型
- `feat`: 新功能
- `fix`: Bug 修復
- `docs`: 文檔更新
- `style`: 樣式調整（不影響功能）
- `refactor`: 重構
- `test`: 測試相關
- `chore`: 其他（建置、工具等）

### 範例
```bash
git commit -m "feat: 新增通知系統功能"
git commit -m "fix: 修復卡片排序問題"
git commit -m "docs: 更新架構指南"
```

---

## ⚠️ 注意事項

### 1. 不要直接修改生產環境
- 永遠在本地測試完成後再推送
- 使用分支來隔離開發工作

### 2. 定期同步 main 分支
```bash
# 在功能分支開發時，定期同步 main 的更新
git checkout main
git pull origin main
git checkout feature/your-feature
git merge main  # 或 git rebase main
```

### 3. 測試環境變數
- 確保 `.env.local` 中的環境變數正確
- Vercel 的環境變數會自動使用，不需要手動設定

### 4. 建置測試
```bash
# 在推送前先測試建置
npm run build

# 如果建置失敗，不要推送
```

---

## 🎯 快速參考

### 日常開發流程
```bash
# 1. 創建分支
git checkout -b feature/my-feature

# 2. 開發
npm run dev
# 在 Browser Preview 中測試

# 3. 提交
git add .
git commit -m "feat: 我的新功能"
git push -u origin feature/my-feature

# 4. 測試完成後合併
git checkout main
git pull origin main
git merge feature/my-feature
git push origin main
```

### 緊急修復流程
```bash
# 1. 直接在 main 分支修復
git checkout main
git pull origin main

# 2. 修復和測試
npm run dev

# 3. 提交和推送
git add .
git commit -m "fix: 緊急修復 XXX 問題"
git push origin main
```

---

## 📚 相關資源

- [Git 官方文檔](https://git-scm.com/doc)
- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Vercel 部署文檔](https://vercel.com/docs)

---

**最後更新**: 2025-01-XX

